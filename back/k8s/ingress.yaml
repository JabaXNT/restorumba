apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: user-microservice-ingress
spec:
  rules:
      - http:
          paths:
            - pathType: Prefix
              path: "/user"
              backend:
                service:
                  name: user-microservice-service
                  port:
                    number: 80